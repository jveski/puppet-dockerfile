require 'spec_helper'

describe "the dockerfile function visitor class" do

  describe "#visit" do
    it "raises an error if the provider doesn't have a build_script method" do
      # TODO
    end

    it "raises an error if the provider's build_script method returns an array" do
      # TODO
    end

    it "calls the provider's build_script method with the build context" do
      # TODO
    end
  end
end
